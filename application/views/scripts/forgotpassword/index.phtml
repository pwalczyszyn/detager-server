<p>Enter your email address that you use with Detager service. Next press Send the button to initiate password change procedure.</p>
<?php 
// First, set the action on the form
$this->form->setAction($this->url(array('action' => 'index')));
echo $this->form;
